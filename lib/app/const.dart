import 'package:flutter/material.dart';

const emptyText = '無內文';
const syncBodyText = '從新聞組同步發文內容中…';
const syncOverviewText = '從新聞組同步%s封發文概述中…';
const syncOverviewFinishText = '同步完成，請重新整理以觀看新發文。';
const syncOverviewTimeoutText = '等候逾時，請稍後再嘗試。';

const mainTextStyle = TextStyle(fontSize: 18);
const subTextStyle = TextStyle(color: Colors.grey, fontSize: 14);
const senderTextStyle = TextStyle(color: Colors.blueAccent, fontSize: 14);
const pinnedTextStyle = TextStyle(fontSize: 16);
const errorTextStyle = TextStyle(color: Colors.redAccent, fontSize: 16);
